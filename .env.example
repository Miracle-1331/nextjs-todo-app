# Copy to .env.local and fill in your values
# When DATABASE_URL is set, the app uses Postgres; otherwise it falls back to the in-memory store.
DATABASE_URL=postgresql://user:password@localhost:5432/todos
